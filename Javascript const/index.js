// constant variable

const pi = 3.14;
let radius;
let circle;
radius = window.prompt("Enter The radius ")
radius = Number(radius);
circle = 2 * pi * radius;
alert("the circle is " + circle);
